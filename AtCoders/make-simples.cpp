#include <bits/stdc++.h>
using namespace std;
// grafos - 
int main(){
    int n,m; // n = vertices | m = arestas
    cin >> n >> m;
    int grafo[m][2];
    for(int i = 0 ; i < m ; i ++){
        for(int = j; j < 1 ; j++){
            cin >> grafo[i][j] >> grafo[i][j+1];
        }
    }
    return 0;
}